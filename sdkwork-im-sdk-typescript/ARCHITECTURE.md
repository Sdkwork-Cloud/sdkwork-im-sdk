# TypeScript Workspace Architecture

This workspace is split into:

- `generated/server-openapi`
- `adapter-wukongim`
- `composed`

The historical handwritten SDK remains migration input. New work should follow the layered structure above.
