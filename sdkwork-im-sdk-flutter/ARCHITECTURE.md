# Flutter Workspace Architecture

This workspace is split into:

- `generated/server-openapi`
- `adapter-wukongim`
- `composed`

Realtime behavior belongs to handwritten layers, not to generated HTTP output.
